# Fixtures

Frozen, public sample data so the kit runs locally with **no SaSame backend**.
Contributors design against these; SaSame deploys against the real (private) backend.
These contain only public display values — never secrets, pricing logic, or keys.
